spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/ibcspri
    username: root
    password:
    testWhileIdle: true
    validationQuery: SELECT 1
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
       hibernate:
       dialect: org.hibernate.dialect.MySQL5Dialect
  
server:
  port: 8092
        
    
    